{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"nodemon server/index.js\" \"cd ./client && npm start\"",
    "build": "concurrently \"node server/index.js\" \"cd ./client && npm build\" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.3.0",
    "express": "^4.17.1",
    "fs-extra": "^10.0.0",
    "mysql2": "^2.3.2",
    "nodemon": "^2.0.14",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "stylelint-config-recommended": "^6.0.0"
  }
}
